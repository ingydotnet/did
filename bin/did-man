#!/usr/bin/env bash

source "$(dirname $BASH_SOURCE)/../lib/use.bash"

main() {
  did_name="$1"
  normalize-did-name
  docker run --rm --entrypoint cat "$did_name" /docker-id/man.1 |
    man -l -
}

[[ $0 != "$BASH_SOURCE" ]] || main "$@"

# vim: set ft=sh lisp:
